name: Frontend CI/CD

on:
  push:
    branches:
      - main

jobs:

  deploy:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
          aws-region: eu-north-1

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build Image
        run: |
          docker build -t frontend .

      - name: Tag Image
        run: |
          docker tag frontend:latest 744420879952.dkr.ecr.eu-north-1.amazonaws.com/frontend:latest

      - name: Push Image
        run: |
          docker push 744420879952.dkr.ecr.eu-north-1.amazonaws.com/frontend:latest

      - name: Deploy EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /opt/microservices

            aws ecr get-login-password --region eu-north-1 | \
            docker login --username AWS --password-stdin 744420879952.dkr.ecr.eu-north-1.amazonaws.com

            docker compose pull frontend

            docker compose up -d frontend
